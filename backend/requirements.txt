# ==============================================================================
# BACKEND DIRECT DEPENDENCIES (Geo-Yield-AI)
# ==============================================================================
# A strict version-pinning policy is applied, using '==' to guarantee exact
# reproducibility (avoiding the "works on my machine" syndrome).

# --- Web framework ---
fastapi==0.135.3
uvicorn==0.43.0
starlette==0.48.0

# --- Data validation and configuration ---
pydantic==2.12.5
python-dotenv==1.2.2

# --- Persistence and database layer ---
SQLAlchemy==2.0.49
psycopg2-binary==2.9.12
alembic==1.19.1
geoalchemy2==0.20.0
pgvector==0.5.0

# --- RAG (Retrieval-Augmented Generation) and legal engine ---
sentence-transformers==5.7.0
anthropic==0.122.0

# --- Orchestrator agent (AI graphs) ---
langgraph==1.2.11

# Optional: only for testing with a free-tier key while the Anthropic account
# gets funded (see backend/rag/gemini_adapter.py). This is not the final
# provider decision -- that remains Claude.
google-genai==2.18.1

# --- Data processing (ETL) ---
# Technical note (risk management): the controlled jump to major version 3.0
# was made after thoroughly verifying its compatibility. The data pipeline's
# integration tests (test_etl.py) certified there are no breaking changes
# relative to the previous 2.x version.
pandas==3.0.2

# --- Observability: direct CloudWatch shipping (OPTIONAL) ---
# Only needed with LOG_CLOUDWATCH_ENABLED=true, i.e. when the host does NOT
# collect stdout (the Render case). On ECS/Fargate/App Runner the host driver
# already ships stdout and these two are unnecessary.
#
# The import is lazy (backend/observability/cloudwatch.py), so the app starts
# fine without them while the flag stays false. Left commented out so boto3
# (~15 MB with transitives) is not imposed on anyone who does not need it.
# boto3==1.41.20
# watchtower==3.5.0

# --- Code quality and testing ---
pytest==8.3.5
pytest-cov==4.1.0
httpx==0.28.1