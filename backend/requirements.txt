# ==============================================================================
# DEPENDENCIAS DIRECTAS DEL BACKEND (Geo-Yield-AI)
# ==============================================================================
# Se aplica una política de fijación estricta de versiones (Version Pinning) 
# usando '==' para garantizar la reproducibilidad exacta (Evitar el síndrome 
# de "en mi máquina sí funciona").

# --- Web framework ---
fastapi==0.135.3
uvicorn==0.43.0
starlette==0.48.0

# --- Validación de datos y Configuración ---
pydantic==2.12.5
python-dotenv==1.2.2

# --- Capa de Persistencia y Base de Datos ---
SQLAlchemy==2.0.49
psycopg2-binary==2.9.12
alembic==1.19.1
geoalchemy2==0.20.0
pgvector==0.5.0

# --- Motor RAG (Retrieval-Augmented Generation) y Legal ---
sentence-transformers==5.7.0
anthropic==0.122.0

# --- Agente Orquestador (Grafos de Inteligencia Artificial) ---
langgraph==1.2.11

# Opcional: solo para pruebas con clave gratuita mientras se financia la
# cuenta de Anthropic (ver backend/rag/gemini_adapter.py). No es la
# decisión final de proveedor -- esa sigue siendo Claude.
google-genai==2.18.1

# --- Procesamiento de Datos (ETL) ---
# Nota Técnica (Gestión de Riesgos): Se ha dado el salto controlado a la versión 
# mayor 3.0 tras verificar exhaustivamente su compatibilidad. Las pruebas de 
# integración (test_etl.py) de la tubería de datos han certificado que no 
# hay rupturas (Breaking Changes) respecto a la versión 2.x anterior.
pandas==3.0.2

# --- Calidad de Código y Testing ---
pytest==8.3.5
pytest-cov==4.1.0
httpx==0.28.1
